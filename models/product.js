class Product {
    constructor(id, ownerId, title,imageUrl, description,  price, rentType, country, region, city, rooms, bathrooms, phonenumber) {
     this.id = id;
     this.ownerId = ownerId;
     this.title = title;
     this.imageUrl = imageUrl;
     this.description = description;
     this.price = price;
     this.rentType = rentType;
     this.country = country;
     this.region = region;
     this.city = city;
     this.rooms = rooms;
     this.bathrooms = bathrooms;
     this.phonenumber = phonenumber;
    }
}

    export default Product; 